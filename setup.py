from setuptools import setup, find_packages

setup(
    name="nerformance",
    version="0.2.0",
    description="A library for evaluating Named Entity Recognition (NER) performance.",
    author="Mehrdad Heshmat",
    packages=find_packages(),
    install_requires=[],  # Add dependencies if any
)